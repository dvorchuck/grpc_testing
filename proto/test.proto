syntax = "proto3";

package transfer;

service TestService {
    rpc Test(TestRequest) returns (TestResponse) {}
}

message TestRequest {
    string data = 1;
}

message TestInfo {
    string a = 1;
    string b = 2;
}

message TestResponse {
    bool success = 1;
}